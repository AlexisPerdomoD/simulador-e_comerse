class producto{
  constructor (codigo, nombre, descripcion, precio, cantidad = 1 ){
    this.codigo = codigo,
    this.nombre = nombre, 
    this.descripcion = descripcion,
    this.precio = precio, 
    this.cantidad = cantidad
  }
}